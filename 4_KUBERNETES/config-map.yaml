apiVersion: v1
kind: ConfigMap
metadata:
	name: example-config
	labels:
		app: example-nginx
data:
	index.html: |
		<html>
		<head><title>Example App</title></head>
		<body>
			<h1>Hello from example-config ConfigMap</h1>
			<p>This content is mounted into the nginx pod.</p>
		</body>
		</html>

